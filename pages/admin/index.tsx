"use client";
import Layout from "@/layout/admin";

interface Props {}

const Admin: React.FC<Props> = (props) => {
  return (
    <Layout>
      <div>
        123
      </div>
    </Layout>
  );
};

export default Admin;
